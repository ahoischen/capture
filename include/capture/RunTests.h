#ifndef CAPTURE_RUNTESTS_H
#define CAPTURE_RUNTESTS_H

#include <string>
#include <vector>

namespace capture {
    int RunTests(const std::vector<std::string> &args);
}

#endif // CAPTURE_RUNTESTS_H
